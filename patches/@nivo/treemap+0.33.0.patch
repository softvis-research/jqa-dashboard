patch-package
--- a/node_modules/@nivo/treemap/es/TreeMapHtmlNode.js
+++ b/node_modules/@nivo/treemap/es/TreeMapHtmlNode.js
@@ -23,6 +23,12 @@ var TreeMapHtmlNode = function TreeMapHtmlNode(_ref) {
     return React.createElement(
         'div',
         _extends({
+            id: (node.data && node.data.data && node.data.data.id) ?
+                    (node.data.data.id + "").replace(/[^\w]/gi, '-') :
+                        ( (node.data && node.data.id) ?
+                            (node.data.id + "").replace(/[^\w]/gi, '-') :
+                            (node.id + "").replace(/[^\w]/gi, '-')
+                        ),
             style: {
                 boxSizing: 'border-box',
                 position: 'absolute',
